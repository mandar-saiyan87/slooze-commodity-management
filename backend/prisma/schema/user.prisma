model User {
    id       Int    @id @default(autoincrement())
    email    String @unique
    password String
    role     Role

    products Product[]

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
